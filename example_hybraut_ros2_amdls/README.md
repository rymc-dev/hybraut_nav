# example hybraut ros2 amdls

This is a data module containing several sample `AMDL's`, utilizing `ACI` implementations as defined in the `hybraut_common_behaviours` module. 
this sample `AMDL`'s can be utilized as base examples for your own development of hybraut behaviours.