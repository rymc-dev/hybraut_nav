# hybraut factory

This is a package which can be utilized for converting DSL files into hybraut_models, 
current we only have one sub-package amdl which converts the dsl for generating automatons called automaton model definition language (yml) files into hybraut_models